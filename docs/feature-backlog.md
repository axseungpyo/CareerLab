# CareerLab — Feature Backlog

> 부분 구현/미구현 기능 정리. 향후 아이디에이션 및 우선순위 결정에 활용.

---

## 1. 부분 구현 (Partially Implemented)

### 1.1 면접 평가 점수 저장/표시

| 항목 | 내용 |
|------|------|
| **현재 상태** | `mock_sessions.evaluation` JSONB에 평가 결과 저장됨. `interview/result/[id]` 페이지에서 전체 점수/등급 표시. 하지만 개별 답변별 점수(`mock_messages.score`, `mock_messages.feedback`)는 저장 로직만 있고 UI 미표시. |
| **관련 파일** | `backend/modules/interview/evaluator.py`, `frontend/app/interview/result/[id]/page.tsx` |
| **필요 작업** | 답변별 점수/피드백을 결과 페이지에 확장 표시. 답변 카드에 점수 배지 + 피드백 텍스트 + 모범답안 토글. |
| **난이도** | 낮음 |
| **예상 작업량** | 프론트엔드 0.5일 |

---

### 1.2 자소서 버전 관리

| 항목 | 내용 |
|------|------|
| **현재 상태** | `resume_items.version` 필드 존재 (integer). 항목 생성 시 version=1로 설정. 하지만 같은 질문에 대해 재생성하면 새 row로 추가되며, 버전 간 비교/복원 UI 없음. |
| **관련 파일** | `backend/modules/resume/models.py` (`version` 필드), `frontend/app/resume/[id]/page.tsx` |
| **필요 작업** | (1) 같은 question에 대한 재생성 시 version 증가 로직, (2) 버전 목록 UI, (3) 이전 버전 복원 기능 |
| **난이도** | 중간 |
| **예상 작업량** | 백엔드 0.5일 + 프론트엔드 1일 |

---

### 1.3 에러 응답 표준화

| 항목 | 내용 |
|------|------|
| **현재 상태** | 각 라우트에서 `HTTPException`으로 에러 반환. 하지만 에러 응답 포맷이 통일되지 않음 — 일부는 `detail` 문자열, 일부는 `{"error": ...}` 딕셔너리. 프론트엔드에서 에러 파싱이 일관되지 않음. |
| **관련 파일** | `backend/api/routes/*.py`, `frontend/lib/api.ts` |
| **필요 작업** | (1) 에러 응답 스키마 정의 (`ErrorResponse` Pydantic 모델), (2) 커스텀 예외 핸들러 미들웨어, (3) 프론트엔드 `api.ts`에서 통일된 에러 파싱 |
| **난이도** | 낮음 |
| **예상 작업량** | 백엔드 0.5일 + 프론트엔드 0.5일 |

---

### 1.4 홈 대시보드

| 항목 | 내용 |
|------|------|
| **현재 상태** | 프로필 진행률 링, 최근 자소서 목록, 빠른 액션 카드 표시. 하지만 상세 분석(지원 현황 추이, 합격률, 면접 점수 추이)은 미구현. |
| **관련 파일** | `frontend/app/page.tsx` |
| **필요 작업** | (1) 통계 API 엔드포인트 추가, (2) 차트 컴포넌트 (recharts/chart.js), (3) 기간별 필터링 |
| **난이도** | 중간 |
| **예상 작업량** | 백엔드 0.5일 + 프론트엔드 1.5일 |

---

### 1.5 리서치 파일 통합

| 항목 | 내용 |
|------|------|
| **현재 상태** | `POST /resume/research/import` 엔드포인트로 리서치 파일 임포트 가능. `GET /resume/research/files`로 목록 조회. 하지만 자소서 생성 파이프라인에 리서치 결과가 자동 주입되지 않음. |
| **관련 파일** | `backend/api/routes/resume.py`, `backend/modules/resume/generator.py` |
| **필요 작업** | 자소서 생성 시 관련 리서치 파일을 컨텍스트에 포함하는 로직 추가 |
| **난이도** | 낮음 |
| **예상 작업량** | 백엔드 0.5일 |

---

## 2. 미구현 — 중요도 높음 (Core Quality)

### 2.1 API 통합 테스트

| 항목 | 내용 |
|------|------|
| **현재 상태** | 단위 테스트 7개 파일 존재 (llm_router, prompt_engine, embedding, settings, oauth, codex). API 엔드포인트 레벨의 통합 테스트는 없음. |
| **관련 파일** | `backend/tests/` |
| **필요 작업** | (1) `httpx.AsyncClient` + `TestClient`로 각 라우트 통합 테스트, (2) LLM 호출은 `respx`로 모킹, (3) Supabase 호출은 fixture로 모킹 |
| **구현 방법** | `tests/test_routes_profile.py`, `test_routes_resume.py`, `test_routes_interview.py` 등 라우트별 테스트 파일 생성 |
| **난이도** | 중간 |
| **예상 작업량** | 2-3일 |

---

### 2.2 요청 타임아웃 및 취소

| 항목 | 내용 |
|------|------|
| **현재 상태** | LLM 호출에 `tenacity` 기반 재시도 (3회, 30초)는 있지만, 개별 요청 타임아웃 설정이 명시적이지 않음. 프론트엔드에서 SSE 스트리밍 취소 UI도 없음. |
| **관련 파일** | `backend/core/llm_router.py`, `frontend/lib/api.ts` |
| **필요 작업** | (1) `httpx` 타임아웃 명시적 설정, (2) SSE 연결 취소 (AbortController), (3) 프론트엔드 "중지" 버튼 |
| **난이도** | 낮음 |
| **예상 작업량** | 1일 |

---

### 2.3 DB 트랜잭션 처리

| 항목 | 내용 |
|------|------|
| **현재 상태** | Supabase-py 클라이언트로 개별 쿼리 실행. 자소서 생성 시 `resumes` + `resume_items` 삽입이 별도 쿼리로 수행되어, 중간 실패 시 불일치 가능. |
| **관련 파일** | `backend/modules/*/repository.py` |
| **필요 작업** | (1) Supabase RPC 함수로 트랜잭션 래핑, (2) 또는 supabase-py의 `.rpc()` 활용 |
| **난이도** | 중간 |
| **예상 작업량** | 1-2일 |

---

### 2.4 입력 유효성 검증 강화

| 항목 | 내용 |
|------|------|
| **현재 상태** | Pydantic 모델로 기본 타입 검증은 됨. 하지만 비즈니스 규칙 (자소서 글자수 제한, 채용공고 최소 길이, 프로필 필수 필드) 검증이 부족. |
| **관련 파일** | `backend/modules/*/models.py`, `frontend/components/` |
| **필요 작업** | (1) Pydantic `field_validator`로 비즈니스 규칙 추가, (2) 프론트엔드 폼 유효성 검증 (zod/react-hook-form) |
| **난이도** | 낮음 |
| **예상 작업량** | 1일 |

---

### 2.5 Rate Limiting

| 항목 | 내용 |
|------|------|
| **현재 상태** | API에 요청 제한 없음. LLM 호출이 비용이 크므로, 악의적/실수 반복 요청에 취약. |
| **관련 파일** | `backend/main.py` |
| **필요 작업** | `slowapi` 또는 커스텀 미들웨어로 엔드포인트별 rate limit 설정 (예: LLM 호출 분당 10회) |
| **난이도** | 낮음 |
| **예상 작업량** | 0.5일 |

---

## 3. 미구현 — 기능 확장 (Feature Enhancement)

### 3.1 멀티 프로필 지원

| 항목 | 내용 |
|------|------|
| **현재 상태** | `profiles` 테이블은 다수 프로필 지원하지만, 코드에서 `get_first_profile()`로 첫 번째만 사용. UI에 프로필 전환 기능 없음. |
| **관련 파일** | `backend/modules/profile/service.py`, `frontend/app/profile/page.tsx` |
| **필요 작업** | (1) 프로필 선택 UI (드롭다운/탭), (2) 컨텍스트별 프로필 ID 전달, (3) 프로필별 데이터 필터링 |
| **난이도** | 중간 |
| **예상 작업량** | 2일 |

---

### 3.2 자소서 버전 비교 (Diff View)

| 항목 | 내용 |
|------|------|
| **현재 상태** | 버전 필드만 존재 (1.2 참고). |
| **필요 작업** | (1) 동일 질문의 버전 목록 API, (2) 두 버전 간 diff 표시 (text-diff 라이브러리), (3) 사이드바이사이드 또는 인라인 diff UI |
| **난이도** | 중간 |
| **예상 작업량** | 2일 |

---

### 3.3 CSV/PDF 내보내기

| 항목 | 내용 |
|------|------|
| **현재 상태** | DOCX 내보내기만 구현됨 (`modules/resume/exporter.py`). |
| **필요 작업** | (1) PDF 내보내기 (WeasyPrint 또는 Playwright PDF), (2) CSV 내보내기 (경력 데이터, 면접 결과), (3) 내보내기 포맷 선택 UI |
| **난이도** | 낮음~중간 |
| **예상 작업량** | 1-2일 |

---

### 3.4 분석 대시보드

| 항목 | 내용 |
|------|------|
| **현재 상태** | 홈 페이지에 기본 요약만 표시 (1.4 참고). |
| **필요 작업** | 별도 `/analytics` 페이지 — (1) 지원 현황 타임라인, (2) 기업별 합격/불합격 추이, (3) 면접 점수 변화 그래프, (4) 자소서 품질 추이 (첨삭 점수), (5) 경력 태그 클라우드 |
| **난이도** | 높음 |
| **예상 작업량** | 3-5일 |

---

### 3.5 자소서 템플릿 라이브러리

| 항목 | 내용 |
|------|------|
| **현재 상태** | 자소서 생성은 매번 처음부터. 이전 성공 사례를 재활용하는 기능 없음. |
| **필요 작업** | (1) 합격 자소서를 템플릿으로 저장, (2) 새 자소서 생성 시 템플릿 선택, (3) 프롬프트에 템플릿 참조 주입 |
| **난이도** | 중간 |
| **예상 작업량** | 2일 |

---

### 3.6 면접 질문 북마크 & 오답노트

| 항목 | 내용 |
|------|------|
| **현재 상태** | 면접 질문 생성/조회는 됨. 하지만 특정 질문을 북마크하거나, 틀린 답변을 모아서 복습하는 기능 없음. |
| **필요 작업** | (1) `interview_questions.bookmarked` 필드 추가, (2) 오답 필터링 (점수 기준), (3) 북마크/오답 목록 UI |
| **난이도** | 낮음 |
| **예상 작업량** | 1일 |

---

### 3.7 알림 및 리마인더

| 항목 | 내용 |
|------|------|
| **현재 상태** | 없음. |
| **필요 작업** | (1) 지원 마감일 설정, (2) 면접 일정 등록, (3) 브라우저 알림 (Notification API) 또는 이메일 발송 |
| **난이도** | 중간 |
| **예상 작업량** | 2-3일 |

---

## 4. 미구현 — 장기 비전 (Future Vision)

### 4.1 음성 모의면접

| 항목 | 내용 |
|------|------|
| **설명** | 텍스트 대신 음성으로 모의면접 진행. STT(음성→텍스트) + TTS(텍스트→음성) 파이프라인. |
| **기술 스택** | Web Speech API (브라우저 STT) + OpenAI TTS 또는 ElevenLabs |
| **난이도** | 높음 |
| **예상 작업량** | 5-7일 |

---

### 4.2 오프라인 모드

| 항목 | 내용 |
|------|------|
| **설명** | PWA로 전환하여 오프라인에서도 프로필/자소서 조회 가능. 온라인 복귀 시 동기화. |
| **기술 스택** | Service Worker + IndexedDB + Background Sync |
| **난이도** | 높음 |
| **예상 작업량** | 5-7일 |

---

### 4.3 협업 기능

| 항목 | 내용 |
|------|------|
| **설명** | 멘토/동료에게 자소서 공유, 코멘트 받기. 피드백 요청/응답 워크플로우. |
| **기술 스택** | Supabase Auth (사용자 인증) + RLS (행 수준 보안) + Realtime (실시간 코멘트) |
| **난이도** | 매우 높음 |
| **예상 작업량** | 2-3주 |

---

### 4.4 AI 자동 지원 추천

| 항목 | 내용 |
|------|------|
| **설명** | 사용자 프로필 기반으로 적합한 채용공고를 자동 수집 및 추천. |
| **기술 스택** | 채용사이트 크롤링/API + 프로필 임베딩 vs 공고 임베딩 시맨틱 매칭 |
| **난이도** | 매우 높음 |
| **예상 작업량** | 2-3주 |

---

### 4.5 면접 영상 분석

| 항목 | 내용 |
|------|------|
| **설명** | 웹캠 녹화 후 표정/시선/자세 분석. 비언어적 커뮤니케이션 피드백. |
| **기술 스택** | MediaRecorder API + Vision LLM (Claude 멀티모달) |
| **난이도** | 매우 높음 |
| **예상 작업량** | 3-4주 |

---

## 5. 우선순위 매트릭스

```
영향도 높음
  │
  │  [2.1 통합 테스트]     [3.4 분석 대시보드]
  │  [2.2 타임아웃/취소]   [4.1 음성 면접]
  │  [1.3 에러 표준화]     [4.4 AI 추천]
  │  [2.5 Rate Limit]
  │
  │  [1.1 면접 점수 UI]    [3.1 멀티 프로필]
  │  [1.2 버전 관리]       [3.5 템플릿]
  │  [2.4 입력 검증]       [3.6 북마크/오답]
  │  [1.5 리서치 통합]     [3.7 알림]
  │
  │  [1.4 대시보드]        [4.2 오프라인]
  │  [2.3 DB 트랜잭션]     [4.3 협업]
  │                        [4.5 영상 분석]
  │
  └───────────────────────────────────────
       구현 용이              구현 어려움
```

---

## 6. 권장 구현 순서

### Phase 1 — 안정화 (1주)
1. 에러 응답 표준화 (1.3)
2. 요청 타임아웃 및 취소 (2.2)
3. Rate Limiting (2.5)
4. 입력 유효성 검증 강화 (2.4)

### Phase 2 — 핵심 개선 (1-2주)
5. API 통합 테스트 (2.1)
6. 면접 평가 점수 UI 확장 (1.1)
7. 자소서 버전 관리 (1.2)
8. 리서치 파일 통합 (1.5)

### Phase 3 — 기능 확장 (2-4주)
9. 멀티 프로필 지원 (3.1)
10. CSV/PDF 내보내기 (3.3)
11. 면접 북마크/오답노트 (3.6)
12. 자소서 템플릿 라이브러리 (3.5)
13. 분석 대시보드 (3.4)

### Phase 4 — 장기 비전 (선택적)
14. 음성 모의면접 (4.1)
15. 알림/리마인더 (3.7)
16. AI 자동 지원 추천 (4.4)
